import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class InterestChecks extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }
  render() {
    return (
      <div>interest checks soon!</div>
    );
  }
}

InterestChecks.propTypes = {
  //   onClickHandler: PropTypes.func
};

InterestChecks.defaultProps = {
  //   onClickHandler: () => null
};

export default InterestChecks;