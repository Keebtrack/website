import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class Vendors extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }
  render() {
    return (
      <div>vendors soon!</div>
    );
  }
}

Vendors.propTypes = {
  //   onClickHandler: PropTypes.func
};

Vendors.defaultProps = {
  //   onClickHandler: () => null
};

export default Vendors;