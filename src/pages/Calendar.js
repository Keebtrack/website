import React, { Component } from 'react';
// import PropTypes from 'prop-types';

class Calendar extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }
  render() {
    return (
      <div>calendar soon!</div>
    );
  }
}

Calendar.propTypes = {
  //   onClickHandler: PropTypes.func
};

Calendar.defaultProps = {
  //   onClickHandler: () => null
};

export default Calendar;